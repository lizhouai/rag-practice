# DeepSeek V4 Pro through Anthropic-compatible Messages API.
ANTHROPIC_API_KEY=sk-your-deepseek-key
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
LLM_MODEL=deepseek-v4-pro
LLM_MAX_TOKENS=1200

# Zhipu embedding-3 through the embeddings endpoint.
ZHIPU_API_KEY=your-zhipu-key
EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4
EMBEDDING_MODEL=embedding-3
EMBEDDING_DIMENSIONS=1024

# Optional local reranker service through FlagEmbedding or Transformers.
# Start `python scripts/serve_bge_reranker.py`, then switch to:
# RERANKER_PROVIDER=flagembedding
# RERANKER_URL=http://127.0.0.1:8008/rerank
# RERANKER_MODEL=bge-reranker-v2-m3
# Optional: pre-download the model and point the service to the local directory.
# RERANKER_MODEL_DIR=D:/models/bge-reranker-v2-m3
# RERANKER_TIMEOUT_SECONDS=30

# Production vector database middleware.
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=production_rag_chunks
# Required for Qdrant Cloud or any authenticated Qdrant endpoint.
# QDRANT_API_KEY=your-qdrant-api-key

# Optional: choose where SQLite index, traces, and metrics are written.
# RAG_RUNTIME_DIR=C:\tmp\production_rag_runtime
